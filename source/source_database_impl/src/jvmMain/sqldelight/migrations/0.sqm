CREATE TABLE articles (
	id INTEGER NOT NULL,
	date INTEGER NOT NULL,
	name TEXT NOT NULL,
	text TEXT NOT NULL,
	PRIMARY KEY (id)
);

CREATE TABLE images (
	id INTEGER NOT NULL,
	image BLOB NOT NULL,
	PRIMARY KEY(id)
);
